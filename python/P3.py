for i in range(4): #act as row
    for j in range(4): # act as col
        print("*", end="")
    print()
    
# we use empty print() stmt to break the continue of * pattern to form next line


# We use print() without arguments to insert a newline, which breaks the current row of stars and starts the next row.